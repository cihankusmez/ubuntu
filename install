# ubuntu
Sıfırdan ubuntu kurulumu

1. Ubuntu 14.04 Kurulumu

2. Google Chrome Kurulumu

3. Apache Kurulumu
      sudo apt-get update
      sudo apt-get install apache2

4. Mysql Kurulumu
      sudo apt-get install mysql-server php5-mysql
      sudo mysql_install_db
      sudo mysql_secure_installation

5. PHP Kurulumu
      sudo apt-get install php5 libapache2-mod-php5 php5-mcrypt

6. Apache'yi tekrar başlat
      sudo service apache2 restarte
      
7. PHP Ekleri Kurmak
      sudo apt-get install php5-cli php5-curl
      
8. Yedekleri PC'ye yükle

9. .rpm dosyalarının kurulumu
      sudo apt-get install alien
      sudo alien my_package.rpm
      sudo dpkg -i my_package.deb

10. JDK 8 Kurulumu
      http://www.oracle.com/technetwork/java/javase/downloads
      
11. .sh Kurulumu
      chmod +x /home/cihan/PhpStorm-162.1121.38/bin/phpstorm.sh
      ./phpstorm.sh

12. PhpStorm Kurulumu

13. PhpStorm Ayar Dosyalarını İçe Aktarma
